package com.example.myapplication2.Kotlin.TDDLesson4

import android.util.Log
import org.junit.Test


internal class CacheDataSourceTest{
   /* @Test
    fun test(){
        val now: Now = FakeNow()
        val cacheDataSource: CacheDataSource = CacheDataSource.Timed(now, lifeTimeMillts = 1000)
    }

    private class FakeNow: Now {
        val time: Long = 0

        override fun now(): Log {
            return time
        }
    }*/
}